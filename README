Comments, criticism and suggestions would be much appreciated, especially concerning how readable the PHP code is.

When an instance of the class is created the output variable $display_svg_graph is automatically created.
